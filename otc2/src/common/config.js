// let web_env = "localhost";

// const domain = window.location.origin;

// if (domain.indexOf("https://www.") >= 0) {
//   web_env = "production";
// }

export const Config = {
  // 接口前缀url
  baseUrl: base.codeUrl,
  imscUrl:base.imscUrl,
  // 版本号
  version: "V 2.8.5.01",
  // 公共密钥
  publicSecret: "echat000",
  // 文件图片前缀url
  fileDownUrl: base.fileUrl
}
